# Local Python defaults work without this file. Hosted environments should set
# these through their secret/configuration manager, never commit real secrets.
APP_ENV=development
CHURN_MODEL_PATH=models/churn_logistic_v1.joblib
DATABASE_URL=sqlite:///data/serving/customer_intelligence.db
PORT=8000
